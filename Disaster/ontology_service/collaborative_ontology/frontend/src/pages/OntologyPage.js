import React from 'react';
import { Container, Typography, Box } from '@mui/material';

function OntologyPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>온톨로지 관리</Typography>
      <Typography color="textSecondary">온톨로지 클래스, 속성, 관계를 관리합니다.</Typography>
    </Box>
  );
}

export default OntologyPage;
