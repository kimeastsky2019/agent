import React from 'react';
import { Container, Typography } from '@mui/material';

function RegisterPage() {
  return (
    <Container>
      <Typography variant="h4">회원가입</Typography>
      <Typography>회원가입 페이지 (구현 예정)</Typography>
    </Container>
  );
}

export default RegisterPage;
